# alpine-archive action

This is a test action that runs `tar -czvf tar_gz_name file_or_dir_path` in
an Alpine container.

## Parameters:

* `tar_gz_name`

  Name of archive file to create
  
* `file_or_dir_path`

  File or directory path to archive
